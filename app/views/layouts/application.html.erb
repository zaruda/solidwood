<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <%= favicon_link_tag asset_path('favicon.jpg') %>
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%=  request.original_url %>" />
    <meta name="referrer" content="always">
    <meta name="theme-color" content="#ffffff">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>

    <% if yield(:title).present? %>
      <title><%= yield(:title) %> | СолидВуд</title>
      <meta property="og:title" content="<%= yield(:title) %> | СолидВуд" />
      <% else %>
        <title>СолидВуд - Пиломатериалы от производителя</title>
        <meta property="og:title" content="СолидВуд - Пиломатериалы от производителя" />
    <% end %>

    <% if yield(:description).present? %>
      <meta name="description" content="<%= yield(:description) %>" />
      <meta property="og:description" content="<%= yield(:description) %>" />
    <% end %>

    <% if yield(:image).present? %>
      <meta property="og:image" content="<%= yield(:image) %>" />
      <% else %>
      <meta property="og:image" content="<%= asset_path('favicon.jpg') %>" />
    <% end %>




    <% if Rails.env.production? %>
      <script type="text/javascript">
          (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
              (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
              m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
          })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
          ga('create', 'UA-132900949-1', 'auto');
      </script>
    <% end %>

  </head>

  <body>
    <%= render 'partials/header' %>

    <%#= render_breadcrumbs :separator => ' / ' %>

    <main class="<%= current_page?(root_path) ? nil : 'withHeader'%>">
      <%= yield %>
    </main>

    <%= render 'partials/footer' %>

    <%= render 'modals/about' %>

    <%= render 'modals/callback' %>

    <%= render 'modals/map' %>

    <%= render 'modals/success' %>


  </body>
</html>
